services:
  - type: web
    name: ai-impact-students
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run app.py --server.port \ --server.headless true
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
